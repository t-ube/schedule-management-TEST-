export type RecordData = {
    recordId: string;
    userId: string;
    date: string;
    duration: number;
    note: string;
    createdAt: string;
    updatedAt: string;
    deletedAt: string | null;
};
